"use client";
import { useSpace } from "@/app/space/space-shell";
export default function SpacePage() { const { space, profile, partner, loading, retry } = useSpace(); if (loading) return <main className="content"><p className="eyebrow">Our Space</p><h1>Loading your space…</h1></main>; if (!space) return <main className="content"><p className="eyebrow">Something went wrong</p><h1>We couldn’t load your space.</h1><button className="button" onClick={retry}>Try again</button></main>; const names = partner ? `${profile?.display_name ?? "You"} & ${partner.display_name}` : space.name; return <main className="content"><p className="eyebrow">Your private place</p><h1>{names}</h1><section className="feature"><p className="eyebrow">Together</p><h2>Your space is ready.</h2><p>Add your relationship details when you’re ready. For now, this is a quiet place that belongs to the two of you.</p></section><section className="section"><p className="eyebrow">Recent activity</p><h2>Nothing here yet</h2><p>Your shared moments and activity will appear here.</p></section></main>; }
